{% extends ':Base:base_with_left_sidebar.html.twig' %}

{% block content %}
    <div class="col-sm-push-2 col-sm-8">
    <div class="panel panel-primary submit-form">
        <div class="panel-heading"><h4>Підтвердження видалення</h4></div>
        <div class="panel-body">
            <h3>Ви дійсно бажаєте видалити запит</h3>
            <h4>{{ charity.title }}?</h4>
        </div>
        <div class="panel-footer">
            <a href="{{ path('charity_manager_index') }}" class=" btn btn-info btn-form">Назад</a>
            <input type="submit" value="Видалити" class="btn btn-danger btn-form pull-right" />
        </div>
    </div>
    </div>
{% endblock %}